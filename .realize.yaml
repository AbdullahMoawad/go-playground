settings:
  legacy:
    force: false
    interval: 0s
schema:
- name: sql-queries
  path: /Users/abdullah/github/sql-queries
  commands:
    vet:
      status: true
    install:
      status: true
    run:
      status: true
  watcher:
    extensions:
    - go
    paths:
    - /
    ignore:
      paths:
      - .git
      - .realize
      - vendor
